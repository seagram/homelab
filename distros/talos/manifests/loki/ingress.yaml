apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: loki-ingress
  namespace: loki
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: loki
      port:
        number: 3100
  tls:
    - hosts:
        - loki
