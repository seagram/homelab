---
all:
  hosts:
    proxmox-ssh:
      ansible_host: 10.0.0.10
      ansible_user: root
      ansible_connection: ssh
      ansible_ssh_pass: "{{ lookup('amazon.aws.aws_ssm', '/homelab/proxmox_root_password') }}"
    proxmox-tailscale:
      ansible_host: 100.85.178.8
      ansible_user: root
      ansible_connection: ssh
